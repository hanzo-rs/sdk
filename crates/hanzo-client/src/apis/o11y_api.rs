/*
 * Hanzo Cloud API
 *
 * Composed from each subsystem's own projection of its router, in the fleet's mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 * Generated by: https://openapi-generator.tech
 */


use reqwest;
use serde::{Deserialize, Serialize, de::Error as _};
use crate::{apis::ResponseContent, models};
use super::{Error, configuration, ContentType};


/// struct for typed errors of method [`delete_v1_o11y_by_wildcard1`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum DeleteV1O11yByWildcard1Error {
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`delete_v1_o11y_reviews_id`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum DeleteV1O11yReviewsIdError {
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`get_v1_o11y_alerts_last`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum GetV1O11yAlertsLastError {
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`get_v1_o11y_api_v2_healthz`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum GetV1O11yApiV2HealthzError {
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`get_v1_o11y_api_v2_livez`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum GetV1O11yApiV2LivezError {
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`get_v1_o11y_api_v2_readyz`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum GetV1O11yApiV2ReadyzError {
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`get_v1_o11y_by_wildcard1`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum GetV1O11yByWildcard1Error {
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`get_v1_o11y_logs`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum GetV1O11yLogsError {
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`get_v1_o11y_metrics`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum GetV1O11yMetricsError {
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`get_v1_o11y_reviews`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum GetV1O11yReviewsError {
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`get_v1_o11y_reviews_id`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum GetV1O11yReviewsIdError {
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`get_v1_o11y_reviews_id_items`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum GetV1O11yReviewsIdItemsError {
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`get_v1_o11y_sessions`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum GetV1O11ySessionsError {
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`get_v1_o11y_status`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum GetV1O11yStatusError {
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`get_v1_o11y_vm_query`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum GetV1O11yVmQueryError {
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`get_v1_o11y_vm_query_range`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum GetV1O11yVmQueryRangeError {
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`options_v1_o11y_by_wildcard1`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum OptionsV1O11yByWildcard1Error {
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`patch_v1_o11y_by_wildcard1`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum PatchV1O11yByWildcard1Error {
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`patch_v1_o11y_reviews_id`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum PatchV1O11yReviewsIdError {
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`patch_v1_o11y_reviews_id_items_item_id`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum PatchV1O11yReviewsIdItemsItemIdError {
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`post_v1_o11y_alerts_by_receiver`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum PostV1O11yAlertsByReceiverError {
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`post_v1_o11y_by_wildcard1`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum PostV1O11yByWildcard1Error {
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`post_v1_o11y_query`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum PostV1O11yQueryError {
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`post_v1_o11y_query_range`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum PostV1O11yQueryRangeError {
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`post_v1_o11y_reviews`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum PostV1O11yReviewsError {
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`post_v1_o11y_reviews_id_items`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum PostV1O11yReviewsIdItemsError {
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`put_v1_o11y_by_wildcard1`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum PutV1O11yByWildcard1Error {
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`trace_v1_o11y_by_wildcard1`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum TraceV1O11yByWildcard1Error {
    UnknownValue(serde_json::Value),
}


/// Removes one of the observability runtime's own objects — a dashboard, an alert rule, a saved view — passing the runtime's answer through unchanged. The fallthrough for the resources only the runtime knows.  A validated, org-scoped principal is required, and the delete is confined to that principal's own tenant, pinned server-side from its claim, so one tenant can never reach another's object. Before the runtime is initialized, 503.
pub async fn delete_v1_o11y_by_wildcard1(configuration: &configuration::Configuration, wildcard1: &str) -> Result<(), Error<DeleteV1O11yByWildcard1Error>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_wildcard1 = wildcard1;

    let uri_str = format!("{}/v1/o11y/{wildcard1}", configuration.base_path, wildcard1=crate::apis::urlencode(p_wildcard1));
    let mut req_builder = configuration.client.request(reqwest::Method::DELETE, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();

    if !status.is_client_error() && !status.is_server_error() {
        Ok(())
    } else {
        let content = resp.text().await?;
        let entity: Option<DeleteV1O11yByWildcard1Error> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent { status, content, entity }))
    }
}

/// DeleteAnnotationQueue removes one review queue and every item in it. A queue id belonging to another org answers the same 404 an unknown id does, so a probe learns nothing about what exists.
pub async fn delete_v1_o11y_reviews_id(configuration: &configuration::Configuration, id: &str) -> Result<models::AnnQueueDeleted, Error<DeleteV1O11yReviewsIdError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_id = id;

    let uri_str = format!("{}/v1/o11y/reviews/{id}", configuration.base_path, id=crate::apis::urlencode(p_id));
    let mut req_builder = configuration.client.request(reqwest::Method::DELETE, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();
    let content_type = resp
        .headers()
        .get("content-type")
        .and_then(|v| v.to_str().ok())
        .unwrap_or("application/octet-stream");
    let content_type = super::ContentType::from(content_type);

    if !status.is_client_error() && !status.is_server_error() {
        let content = resp.text().await?;
        match content_type {
            ContentType::Json => serde_json::from_str(&content).map_err(Error::from),
            ContentType::Text => return Err(Error::from(serde_json::Error::custom("Received `text/plain` content type response that cannot be converted to `models::AnnQueueDeleted`"))),
            ContentType::Unsupported(unknown_type) => return Err(Error::from(serde_json::Error::custom(format!("Received `{unknown_type}` content type response that cannot be converted to `models::AnnQueueDeleted`")))),
        }
    } else {
        let content = resp.text().await?;
        let entity: Option<DeleteV1O11yReviewsIdError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent { status, content, entity }))
    }
}

/// Answers the most recent Alertmanager deliveries THIS process received, as plain text — one greppable `PAGE-DELIVERED` line per alert, newest last, so piping to `tail` reads in arrival order. `(none)` when nothing has landed.  It answers the question Alertmanager cannot: Alertmanager can tell you it dispatched a notification, never that anything received it. This ring is the far side of that hop, and it is the only record that a page actually arrived.  The ring is PROCESS-LOCAL and bounded to the last 200 lines. Both are the point: a receipt that outlived the process that took the call would be a claim about something nobody observed, and an unbounded receipt log is a memory leak with a nice name. A restart empties it.
pub async fn get_v1_o11y_alerts_last(configuration: &configuration::Configuration, ) -> Result<(), Error<GetV1O11yAlertsLastError>> {

    let uri_str = format!("{}/v1/o11y/alerts/last", configuration.base_path);
    let mut req_builder = configuration.client.request(reqwest::Method::GET, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();

    if !status.is_client_error() && !status.is_server_error() {
        Ok(())
    } else {
        let content = resp.text().await?;
        let entity: Option<GetV1O11yAlertsLastError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent { status, content, entity }))
    }
}

/// Reports whether every service in the runtime's registry is healthy, and names them grouped by state — so a failure says WHICH component is down, not merely that something is. An unhealthy registry answers 503, not a 200 with a false flag inside, so a plain status check cannot read a sick runtime as well.  UNAUTHENTICATED by design, like the other two probes: it carries no tenant data and is reached by k8s and by external checks that hold no principal.
pub async fn get_v1_o11y_api_v2_healthz(configuration: &configuration::Configuration, ) -> Result<(), Error<GetV1O11yApiV2HealthzError>> {

    let uri_str = format!("{}/v1/o11y/api/v2/healthz", configuration.base_path);
    let mut req_builder = configuration.client.request(reqwest::Method::GET, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();

    if !status.is_client_error() && !status.is_server_error() {
        Ok(())
    } else {
        let content = resp.text().await?;
        let entity: Option<GetV1O11yApiV2HealthzError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent { status, content, entity }))
    }
}

/// Answers 200 unconditionally while the process is running, and asserts NOTHING about the telemetry stores behind it. That is what makes it a liveness probe: a container that answers this is worth leaving alive, and restarting on a store outage would only remove the thing reporting the outage.  UNAUTHENTICATED by design, and one of exactly three /v1/o11y paths that are. It carries no tenant data, and gating it would break the k8s probes and the external health checks without protecting anything. Use the health probe, not this one, to ask whether the runtime can actually serve.
pub async fn get_v1_o11y_api_v2_livez(configuration: &configuration::Configuration, ) -> Result<(), Error<GetV1O11yApiV2LivezError>> {

    let uri_str = format!("{}/v1/o11y/api/v2/livez", configuration.base_path);
    let mut req_builder = configuration.client.request(reqwest::Method::GET, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();

    if !status.is_client_error() && !status.is_server_error() {
        Ok(())
    } else {
        let content = resp.text().await?;
        let entity: Option<GetV1O11yApiV2LivezError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent { status, content, entity }))
    }
}

/// Reports whether the runtime's registered services are healthy enough to take traffic, and answers 503 when they are not — which is what takes a booting or degraded replica out of the load balancer instead of letting it serve errors.  UNAUTHENTICATED by design, like the other two probes. It reads the same service registry the health probe reads, so the two agree by construction; readiness is the question a router asks and health is the question an operator asks.
pub async fn get_v1_o11y_api_v2_readyz(configuration: &configuration::Configuration, ) -> Result<(), Error<GetV1O11yApiV2ReadyzError>> {

    let uri_str = format!("{}/v1/o11y/api/v2/readyz", configuration.base_path);
    let mut req_builder = configuration.client.request(reqwest::Method::GET, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();

    if !status.is_client_error() && !status.is_server_error() {
        Ok(())
    } else {
        let content = resp.text().await?;
        let entity: Option<GetV1O11yApiV2ReadyzError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent { status, content, entity }))
    }
}

/// Serves the observability runtime's own read surface — dashboards, alert rules, saved views, the service and dependency inventory, and the trace, log and metric explorers — in the runtime's own shapes, passed through unchanged.  It is the FALLTHROUGH, not the front door. Every path this repo owns the shape of is registered ahead of it and wins the match; what reaches here is what only the runtime knows how to answer. The public contract is flat — one /v1/, no nested version — and the mapping onto the runtime's internal namespace happens at this one seam, so a caller never spells an engine version.  A validated principal is required and the read is scoped to that principal's own org, pinned server-side from its claim; a client-supplied org header never survives ingress and there is no query parameter that widens the scope. Platform sudo passes without an org — the admin console reads before one is selected — and buys reach, not data: the runtime still scopes every read from the tenant it was given, so an org-less request answers org-less, never the fleet. Before the runtime is initialized, 503.
pub async fn get_v1_o11y_by_wildcard1(configuration: &configuration::Configuration, wildcard1: &str) -> Result<(), Error<GetV1O11yByWildcard1Error>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_wildcard1 = wildcard1;

    let uri_str = format!("{}/v1/o11y/{wildcard1}", configuration.base_path, wildcard1=crate::apis::urlencode(p_wildcard1));
    let mut req_builder = configuration.client.request(reqwest::Method::GET, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();

    if !status.is_client_error() && !status.is_server_error() {
        Ok(())
    } else {
        let content = resp.text().await?;
        let entity: Option<GetV1O11yByWildcard1Error> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent { status, content, entity }))
    }
}

/// GetO11yLogs returns a page of one product's logs for the caller's org. A normal caller sees its OWN request stream, derived from org-tagged spans; a validated platform SuperAdmin sees the product's raw infra stdout stream instead. Poll for a live tail by passing the previous response's nextCursor back as sinceNs. A well-formed product with no backing workload answers an empty page rather than an error; a malformed slug is a 400.
pub async fn get_v1_o11y_logs(configuration: &configuration::Configuration, product: Option<&str>, since_ns: Option<i32>, window: Option<i32>, limit: Option<i32>) -> Result<models::LogsResponse, Error<GetV1O11yLogsError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_product = product;
    let p_since_ns = since_ns;
    let p_window = window;
    let p_limit = limit;

    let uri_str = format!("{}/v1/o11y/logs", configuration.base_path);
    let mut req_builder = configuration.client.request(reqwest::Method::GET, &uri_str);

    if let Some(ref param_value) = p_product {
        req_builder = req_builder.query(&[("product", &param_value.to_string())]);
    }
    if let Some(ref param_value) = p_since_ns {
        req_builder = req_builder.query(&[("sinceNs", &param_value.to_string())]);
    }
    if let Some(ref param_value) = p_window {
        req_builder = req_builder.query(&[("window", &param_value.to_string())]);
    }
    if let Some(ref param_value) = p_limit {
        req_builder = req_builder.query(&[("limit", &param_value.to_string())]);
    }
    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();
    let content_type = resp
        .headers()
        .get("content-type")
        .and_then(|v| v.to_str().ok())
        .unwrap_or("application/octet-stream");
    let content_type = super::ContentType::from(content_type);

    if !status.is_client_error() && !status.is_server_error() {
        let content = resp.text().await?;
        match content_type {
            ContentType::Json => serde_json::from_str(&content).map_err(Error::from),
            ContentType::Text => return Err(Error::from(serde_json::Error::custom("Received `text/plain` content type response that cannot be converted to `models::LogsResponse`"))),
            ContentType::Unsupported(unknown_type) => return Err(Error::from(serde_json::Error::custom(format!("Received `{unknown_type}` content type response that cannot be converted to `models::LogsResponse`")))),
        }
    } else {
        let content = resp.text().await?;
        let entity: Option<GetV1O11yLogsError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent { status, content, entity }))
    }
}

/// GetO11yMetrics returns one product's RED series — request rate, errors, p50 and p95 latency — for the caller's org, plus that org's LLM usage rollup over the same window. The series come from org-tagged request spans, so a tenant only ever aggregates its own traffic; a validated platform SuperAdmin sees the whole product's RED, while usage stays the caller's own org either way. A well-formed product with no backing workload answers empty series; a malformed slug is a 400.
pub async fn get_v1_o11y_metrics(configuration: &configuration::Configuration, product: Option<&str>, range: Option<i32>, step_sec: Option<i32>) -> Result<models::MetricsResponse, Error<GetV1O11yMetricsError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_product = product;
    let p_range = range;
    let p_step_sec = step_sec;

    let uri_str = format!("{}/v1/o11y/metrics", configuration.base_path);
    let mut req_builder = configuration.client.request(reqwest::Method::GET, &uri_str);

    if let Some(ref param_value) = p_product {
        req_builder = req_builder.query(&[("product", &param_value.to_string())]);
    }
    if let Some(ref param_value) = p_range {
        req_builder = req_builder.query(&[("range", &param_value.to_string())]);
    }
    if let Some(ref param_value) = p_step_sec {
        req_builder = req_builder.query(&[("stepSec", &param_value.to_string())]);
    }
    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();
    let content_type = resp
        .headers()
        .get("content-type")
        .and_then(|v| v.to_str().ok())
        .unwrap_or("application/octet-stream");
    let content_type = super::ContentType::from(content_type);

    if !status.is_client_error() && !status.is_server_error() {
        let content = resp.text().await?;
        match content_type {
            ContentType::Json => serde_json::from_str(&content).map_err(Error::from),
            ContentType::Text => return Err(Error::from(serde_json::Error::custom("Received `text/plain` content type response that cannot be converted to `models::MetricsResponse`"))),
            ContentType::Unsupported(unknown_type) => return Err(Error::from(serde_json::Error::custom(format!("Received `{unknown_type}` content type response that cannot be converted to `models::MetricsResponse`")))),
        }
    } else {
        let content = resp.text().await?;
        let entity: Option<GetV1O11yMetricsError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent { status, content, entity }))
    }
}

/// ListAnnotationQueues returns a page of the caller org's human-review queues, newest first, narrowed to the caller's project. Another org's queues are never visible.
pub async fn get_v1_o11y_reviews(configuration: &configuration::Configuration, page: Option<i32>, limit: Option<i32>) -> Result<models::AnnQueueList, Error<GetV1O11yReviewsError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_page = page;
    let p_limit = limit;

    let uri_str = format!("{}/v1/o11y/reviews", configuration.base_path);
    let mut req_builder = configuration.client.request(reqwest::Method::GET, &uri_str);

    if let Some(ref param_value) = p_page {
        req_builder = req_builder.query(&[("page", &param_value.to_string())]);
    }
    if let Some(ref param_value) = p_limit {
        req_builder = req_builder.query(&[("limit", &param_value.to_string())]);
    }
    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();
    let content_type = resp
        .headers()
        .get("content-type")
        .and_then(|v| v.to_str().ok())
        .unwrap_or("application/octet-stream");
    let content_type = super::ContentType::from(content_type);

    if !status.is_client_error() && !status.is_server_error() {
        let content = resp.text().await?;
        match content_type {
            ContentType::Json => serde_json::from_str(&content).map_err(Error::from),
            ContentType::Text => return Err(Error::from(serde_json::Error::custom("Received `text/plain` content type response that cannot be converted to `models::AnnQueueList`"))),
            ContentType::Unsupported(unknown_type) => return Err(Error::from(serde_json::Error::custom(format!("Received `{unknown_type}` content type response that cannot be converted to `models::AnnQueueList`")))),
        }
    } else {
        let content = resp.text().await?;
        let entity: Option<GetV1O11yReviewsError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent { status, content, entity }))
    }
}

/// GetAnnotationQueue returns one review queue with its pending and completed counts and its first page of items. A queue id belonging to another org is a 404, never a cross-tenant read.
pub async fn get_v1_o11y_reviews_id(configuration: &configuration::Configuration, id: &str) -> Result<models::AnnQueueDetailView, Error<GetV1O11yReviewsIdError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_id = id;

    let uri_str = format!("{}/v1/o11y/reviews/{id}", configuration.base_path, id=crate::apis::urlencode(p_id));
    let mut req_builder = configuration.client.request(reqwest::Method::GET, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();
    let content_type = resp
        .headers()
        .get("content-type")
        .and_then(|v| v.to_str().ok())
        .unwrap_or("application/octet-stream");
    let content_type = super::ContentType::from(content_type);

    if !status.is_client_error() && !status.is_server_error() {
        let content = resp.text().await?;
        match content_type {
            ContentType::Json => serde_json::from_str(&content).map_err(Error::from),
            ContentType::Text => return Err(Error::from(serde_json::Error::custom("Received `text/plain` content type response that cannot be converted to `models::AnnQueueDetailView`"))),
            ContentType::Unsupported(unknown_type) => return Err(Error::from(serde_json::Error::custom(format!("Received `{unknown_type}` content type response that cannot be converted to `models::AnnQueueDetailView`")))),
        }
    } else {
        let content = resp.text().await?;
        let entity: Option<GetV1O11yReviewsIdError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent { status, content, entity }))
    }
}

/// ListAnnotationQueueItems returns a page of one review queue's items, newest first, optionally filtered to PENDING or COMPLETED. A queue id belonging to another org is a 404, never a cross-tenant list.
pub async fn get_v1_o11y_reviews_id_items(configuration: &configuration::Configuration, id: &str, status: Option<&str>, page: Option<i32>, limit: Option<i32>) -> Result<models::AnnItemList, Error<GetV1O11yReviewsIdItemsError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_id = id;
    let p_status = status;
    let p_page = page;
    let p_limit = limit;

    let uri_str = format!("{}/v1/o11y/reviews/{id}/items", configuration.base_path, id=crate::apis::urlencode(p_id));
    let mut req_builder = configuration.client.request(reqwest::Method::GET, &uri_str);

    if let Some(ref param_value) = p_status {
        req_builder = req_builder.query(&[("status", &param_value.to_string())]);
    }
    if let Some(ref param_value) = p_page {
        req_builder = req_builder.query(&[("page", &param_value.to_string())]);
    }
    if let Some(ref param_value) = p_limit {
        req_builder = req_builder.query(&[("limit", &param_value.to_string())]);
    }
    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();
    let content_type = resp
        .headers()
        .get("content-type")
        .and_then(|v| v.to_str().ok())
        .unwrap_or("application/octet-stream");
    let content_type = super::ContentType::from(content_type);

    if !status.is_client_error() && !status.is_server_error() {
        let content = resp.text().await?;
        match content_type {
            ContentType::Json => serde_json::from_str(&content).map_err(Error::from),
            ContentType::Text => return Err(Error::from(serde_json::Error::custom("Received `text/plain` content type response that cannot be converted to `models::AnnItemList`"))),
            ContentType::Unsupported(unknown_type) => return Err(Error::from(serde_json::Error::custom(format!("Received `{unknown_type}` content type response that cannot be converted to `models::AnnItemList`")))),
        }
    } else {
        let content = resp.text().await?;
        let entity: Option<GetV1O11yReviewsIdItemsError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent { status, content, entity }))
    }
}

/// Answers the caller org's LLM-observability sessions — traces grouped by session id on the gen_ai span plane — paged by limit and offset, in the runtime's own envelope, passed through unchanged.  An org-less caller is refused HERE, at the cloud boundary, before the request reaches the runtime, and the org the runtime then scopes on is that SAME validated tenant. The two cannot disagree: the tenant is minted from the principal's own claim at ingress and a client copy never survives it.  There is deliberately no session-detail route to pair with this. The runtime serves the list only; detail is composed client-side from this list plus the traces filtered by session, so a caller looking for one is looking for something that was never served rather than something that broke.
pub async fn get_v1_o11y_sessions(configuration: &configuration::Configuration, ) -> Result<(), Error<GetV1O11ySessionsError>> {

    let uri_str = format!("{}/v1/o11y/sessions", configuration.base_path);
    let mut req_builder = configuration.client.request(reqwest::Method::GET, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();

    if !status.is_client_error() && !status.is_server_error() {
        Ok(())
    } else {
        let content = resp.text().await?;
        let entity: Option<GetV1O11ySessionsError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent { status, content, entity }))
    }
}

/// GetO11yStatus reports whether a product's service is live: an in-cluster health probe with its measured latency, fused with the per-replica up inventory. Infra health is not tenant-partitioned — a service is up or down for everyone — so any validated caller is served, but an unvalidated one is refused. A product with no backing workload answers down/unknown-service without probing anything; a malformed slug is a 400.
pub async fn get_v1_o11y_status(configuration: &configuration::Configuration, product: Option<&str>) -> Result<models::StatusResult, Error<GetV1O11yStatusError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_product = product;

    let uri_str = format!("{}/v1/o11y/status", configuration.base_path);
    let mut req_builder = configuration.client.request(reqwest::Method::GET, &uri_str);

    if let Some(ref param_value) = p_product {
        req_builder = req_builder.query(&[("product", &param_value.to_string())]);
    }
    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();
    let content_type = resp
        .headers()
        .get("content-type")
        .and_then(|v| v.to_str().ok())
        .unwrap_or("application/octet-stream");
    let content_type = super::ContentType::from(content_type);

    if !status.is_client_error() && !status.is_server_error() {
        let content = resp.text().await?;
        match content_type {
            ContentType::Json => serde_json::from_str(&content).map_err(Error::from),
            ContentType::Text => return Err(Error::from(serde_json::Error::custom("Received `text/plain` content type response that cannot be converted to `models::StatusResult`"))),
            ContentType::Unsupported(unknown_type) => return Err(Error::from(serde_json::Error::custom(format!("Received `{unknown_type}` content type response that cannot be converted to `models::StatusResult`")))),
        }
    } else {
        let content = resp.text().await?;
        let entity: Option<GetV1O11yStatusError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent { status, content, entity }))
    }
}

/// Answers VictoriaMetrics' native Prometheus JSON for one instant query, byte-for-byte — its own status code and envelope, unwrapped and un-reshaped, so the console's infrastructure-health board parses the response it was written against.  PLATFORM SUDO ONLY. This is not tenant data: it is the whole fleet's `up{}` inventory, so it takes the same platform-sudo predicate the infra-log god-view takes, and every customer is 403. VictoriaMetrics has no per-request auth of its own — it is an internal ClusterIP — so this handler IS the access boundary, and it fails closed at every step.  The query is ALLOWLISTED, not passed through. The `query` parameter must equal one of the exact PromQL strings the boards issue, or it is a 400; there is no way to phrase a new one. That is what keeps a read proxy from becoming a generic PromQL exfiltration and DoS endpoint.
pub async fn get_v1_o11y_vm_query(configuration: &configuration::Configuration, ) -> Result<(), Error<GetV1O11yVmQueryError>> {

    let uri_str = format!("{}/v1/o11y/vm/query", configuration.base_path);
    let mut req_builder = configuration.client.request(reqwest::Method::GET, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();

    if !status.is_client_error() && !status.is_server_error() {
        Ok(())
    } else {
        let content = resp.text().await?;
        let entity: Option<GetV1O11yVmQueryError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent { status, content, entity }))
    }
}

/// Answers VictoriaMetrics' native Prometheus JSON for one ranged query over start/end/step, byte-for-byte — its own status code and envelope, so the console's infrastructure trends render off the response as-is.  PLATFORM SUDO ONLY, on the same fleet-wide `up{}` inventory and the same fixed board queries as the instant read; a customer is 403. VictoriaMetrics carries no per-request auth, so this handler is the access boundary.  The query is ALLOWLISTED to the exact PromQL the boards issue — anything else is a 400 — and start, end and step must each be positive integers before the request is forwarded. Range arguments are the other half of the same boundary: an unbounded step over an unbounded window is a DoS whether or not the query is on the list.
pub async fn get_v1_o11y_vm_query_range(configuration: &configuration::Configuration, ) -> Result<(), Error<GetV1O11yVmQueryRangeError>> {

    let uri_str = format!("{}/v1/o11y/vm/query_range", configuration.base_path);
    let mut req_builder = configuration.client.request(reqwest::Method::GET, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();

    if !status.is_client_error() && !status.is_server_error() {
        Ok(())
    } else {
        let content = resp.text().await?;
        let entity: Option<GetV1O11yVmQueryRangeError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent { status, content, entity }))
    }
}

/// Published because this address accepts every method, but the runtime routes nothing here: the request reaches it as an unrouted path and no telemetry is read or written.
pub async fn options_v1_o11y_by_wildcard1(configuration: &configuration::Configuration, wildcard1: &str) -> Result<(), Error<OptionsV1O11yByWildcard1Error>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_wildcard1 = wildcard1;

    let uri_str = format!("{}/v1/o11y/{wildcard1}", configuration.base_path, wildcard1=crate::apis::urlencode(p_wildcard1));
    let mut req_builder = configuration.client.request(reqwest::Method::OPTIONS, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();

    if !status.is_client_error() && !status.is_server_error() {
        Ok(())
    } else {
        let content = resp.text().await?;
        let entity: Option<OptionsV1O11yByWildcard1Error> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent { status, content, entity }))
    }
}

/// Applies a partial update to one of the observability runtime's own objects, in the runtime's own shapes, passed through unchanged. The fallthrough for the resources only the runtime knows.  A validated, org-scoped principal is required, and the write is confined to that principal's own tenant, pinned server-side from its claim. Before the runtime is initialized, 503.
pub async fn patch_v1_o11y_by_wildcard1(configuration: &configuration::Configuration, wildcard1: &str) -> Result<(), Error<PatchV1O11yByWildcard1Error>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_wildcard1 = wildcard1;

    let uri_str = format!("{}/v1/o11y/{wildcard1}", configuration.base_path, wildcard1=crate::apis::urlencode(p_wildcard1));
    let mut req_builder = configuration.client.request(reqwest::Method::PATCH, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();

    if !status.is_client_error() && !status.is_server_error() {
        Ok(())
    } else {
        let content = resp.text().await?;
        let entity: Option<PatchV1O11yByWildcard1Error> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent { status, content, entity }))
    }
}

/// UpdateAnnotationQueue changes a review queue's name, description or score-config set. A field the request omits is left alone. A name another queue in the same project already uses is a 409; a queue id belonging to another org is a 404.
pub async fn patch_v1_o11y_reviews_id(configuration: &configuration::Configuration, id: &str, update_queue_in: models::UpdateQueueIn) -> Result<models::AnnQueueView, Error<PatchV1O11yReviewsIdError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_id = id;
    let p_update_queue_in = update_queue_in;

    let uri_str = format!("{}/v1/o11y/reviews/{id}", configuration.base_path, id=crate::apis::urlencode(p_id));
    let mut req_builder = configuration.client.request(reqwest::Method::PATCH, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }
    req_builder = req_builder.json(&p_update_queue_in);

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();
    let content_type = resp
        .headers()
        .get("content-type")
        .and_then(|v| v.to_str().ok())
        .unwrap_or("application/octet-stream");
    let content_type = super::ContentType::from(content_type);

    if !status.is_client_error() && !status.is_server_error() {
        let content = resp.text().await?;
        match content_type {
            ContentType::Json => serde_json::from_str(&content).map_err(Error::from),
            ContentType::Text => return Err(Error::from(serde_json::Error::custom("Received `text/plain` content type response that cannot be converted to `models::AnnQueueView`"))),
            ContentType::Unsupported(unknown_type) => return Err(Error::from(serde_json::Error::custom(format!("Received `{unknown_type}` content type response that cannot be converted to `models::AnnQueueView`")))),
        }
    } else {
        let content = resp.text().await?;
        let entity: Option<PatchV1O11yReviewsIdError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent { status, content, entity }))
    }
}

/// UpdateAnnotationQueueItem moves one queue item between PENDING and COMPLETED and sets its assignee. Completing an item stamps its completedAt. An item that exists under a different queue answers the same 404 an unknown item does, and so does a queue belonging to another org.
pub async fn patch_v1_o11y_reviews_id_items_item_id(configuration: &configuration::Configuration, id: &str, item_id: &str, update_item_in: models::UpdateItemIn) -> Result<models::AnnItemView, Error<PatchV1O11yReviewsIdItemsItemIdError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_id = id;
    let p_item_id = item_id;
    let p_update_item_in = update_item_in;

    let uri_str = format!("{}/v1/o11y/reviews/{id}/items/{itemId}", configuration.base_path, id=crate::apis::urlencode(p_id), itemId=crate::apis::urlencode(p_item_id));
    let mut req_builder = configuration.client.request(reqwest::Method::PATCH, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }
    req_builder = req_builder.json(&p_update_item_in);

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();
    let content_type = resp
        .headers()
        .get("content-type")
        .and_then(|v| v.to_str().ok())
        .unwrap_or("application/octet-stream");
    let content_type = super::ContentType::from(content_type);

    if !status.is_client_error() && !status.is_server_error() {
        let content = resp.text().await?;
        match content_type {
            ContentType::Json => serde_json::from_str(&content).map_err(Error::from),
            ContentType::Text => return Err(Error::from(serde_json::Error::custom("Received `text/plain` content type response that cannot be converted to `models::AnnItemView`"))),
            ContentType::Unsupported(unknown_type) => return Err(Error::from(serde_json::Error::custom(format!("Received `{unknown_type}` content type response that cannot be converted to `models::AnnItemView`")))),
        }
    } else {
        let content = resp.text().await?;
        let entity: Option<PatchV1O11yReviewsIdItemsItemIdError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent { status, content, entity }))
    }
}

/// Records one Alertmanager webhook delivery and pages the on-call. Each alert in the payload prints a `PAGE-DELIVERED` line to the process log and joins the ring the receipt replay serves, then the batch is posted to Slack with the org's KMS-custodied bot token — the ONE Slack egress the product already has, not a second webhook credential. Resolved notifications page too: \"it recovered\" is the half of an incident people are actually waiting for.  It ALWAYS answers 200 with the body `ok`, and a body that will not parse is recorded with empty fields rather than rejected. Alertmanager retries on any other status, so a receipt that pushes back changes the thing it is measuring, and a 400 on a malformed payload would make it retry forever — the delivery still happened, which is the fact being recorded.  The receiver segment is Alertmanager's own receiver name, a parameter rather than a hand-listed route because the receiver set is config, not code. Paging is detached and fail-soft: with no channel configured nothing is posted and the receipt still lands, and a Slack failure prints its own line instead of failing the request.
pub async fn post_v1_o11y_alerts_by_receiver(configuration: &configuration::Configuration, receiver: &str) -> Result<(), Error<PostV1O11yAlertsByReceiverError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_receiver = receiver;

    let uri_str = format!("{}/v1/o11y/alerts/{receiver}", configuration.base_path, receiver=crate::apis::urlencode(p_receiver));
    let mut req_builder = configuration.client.request(reqwest::Method::POST, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();

    if !status.is_client_error() && !status.is_server_error() {
        Ok(())
    } else {
        let content = resp.text().await?;
        let entity: Option<PostV1O11yAlertsByReceiverError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent { status, content, entity }))
    }
}

/// Carries the runtime's own writes and query posts — creating a dashboard, an alert rule or a saved view, and running the query bodies the explorers submit — in the runtime's own shapes, passed through unchanged.  It is the FALLTHROUGH: the builder query and the ingest routes this repo owns are registered ahead of it and win the match. A validated, org-scoped principal is required and the write lands in that principal's own tenant, pinned server-side.  ONE EXEMPTION, and it is deliberate: a Sentry error-ingest write presents a DSN public key, never a Hanzo session, so those two paths bypass the principal gate and are authenticated by the ingest verifier instead — which derives the org from the DSN itself and fails closed. The exemption is matched by method plus prefix plus suffix, never a broad prefix, so every read under the same subtree stays gated. Before the runtime is initialized, 503.
pub async fn post_v1_o11y_by_wildcard1(configuration: &configuration::Configuration, wildcard1: &str) -> Result<(), Error<PostV1O11yByWildcard1Error>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_wildcard1 = wildcard1;

    let uri_str = format!("{}/v1/o11y/{wildcard1}", configuration.base_path, wildcard1=crate::apis::urlencode(p_wildcard1));
    let mut req_builder = configuration.client.request(reqwest::Method::POST, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();

    if !status.is_client_error() && !status.is_server_error() {
        Ok(())
    } else {
        let content = resp.text().await?;
        let entity: Option<PostV1O11yByWildcard1Error> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent { status, content, entity }))
    }
}

/// Runs the console's composite builder query and answers the engine's own response untouched — body, status and headers ride through both ways, because the shape here is the query engine's, not this layer's.  This flat path is the ONE canonical public address for the builder query, and pinning it server-side is the point: the version-less alias resolves to the engine's highest version, which rejects the v3-shaped composite payload the console speaks. So the engine version is resolved INSIDE the handler and the client never names one — a caller that spells a version into the path is coupling itself to an internal detail that is free to move.  Requires a validated principal, and the tenant is the principal's own org, pinned server-side from the validated claim; there is no org selector in the payload or the query string that could widen it. Before the runtime is initialized this answers 503 rather than an empty result.
pub async fn post_v1_o11y_query(configuration: &configuration::Configuration, ) -> Result<(), Error<PostV1O11yQueryError>> {

    let uri_str = format!("{}/v1/o11y/query", configuration.base_path);
    let mut req_builder = configuration.client.request(reqwest::Method::POST, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();

    if !status.is_client_error() && !status.is_server_error() {
        Ok(())
    } else {
        let content = resp.text().await?;
        let entity: Option<PostV1O11yQueryError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent { status, content, entity }))
    }
}

/// Runs the console's composite builder query over a time range — the list and series the trace, log and metric explorers render — and answers the engine's own response untouched, body, status and headers alike.  Same pin as the instant form and for the same reason: the flat path resolves to a specific engine version INSIDE the handler, because the version-less alias resolves to one that rejects the composite payload the console sends. The client speaks only this path.  Requires a validated principal, and the tenant is that principal's own org, pinned server-side; nothing in the request can widen it. Before the runtime is initialized this answers 503.
pub async fn post_v1_o11y_query_range(configuration: &configuration::Configuration, ) -> Result<(), Error<PostV1O11yQueryRangeError>> {

    let uri_str = format!("{}/v1/o11y/query_range", configuration.base_path);
    let mut req_builder = configuration.client.request(reqwest::Method::POST, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();

    if !status.is_client_error() && !status.is_server_error() {
        Ok(())
    } else {
        let content = resp.text().await?;
        let entity: Option<PostV1O11yQueryRangeError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent { status, content, entity }))
    }
}

/// CreateAnnotationQueue creates a human-review queue in the caller's org and project. A name already used by another queue in the same project is a 409.
pub async fn post_v1_o11y_reviews(configuration: &configuration::Configuration, create_queue_req: models::CreateQueueReq) -> Result<models::AnnQueueView, Error<PostV1O11yReviewsError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_create_queue_req = create_queue_req;

    let uri_str = format!("{}/v1/o11y/reviews", configuration.base_path);
    let mut req_builder = configuration.client.request(reqwest::Method::POST, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }
    req_builder = req_builder.json(&p_create_queue_req);

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();
    let content_type = resp
        .headers()
        .get("content-type")
        .and_then(|v| v.to_str().ok())
        .unwrap_or("application/octet-stream");
    let content_type = super::ContentType::from(content_type);

    if !status.is_client_error() && !status.is_server_error() {
        let content = resp.text().await?;
        match content_type {
            ContentType::Json => serde_json::from_str(&content).map_err(Error::from),
            ContentType::Text => return Err(Error::from(serde_json::Error::custom("Received `text/plain` content type response that cannot be converted to `models::AnnQueueView`"))),
            ContentType::Unsupported(unknown_type) => return Err(Error::from(serde_json::Error::custom(format!("Received `{unknown_type}` content type response that cannot be converted to `models::AnnQueueView`")))),
        }
    } else {
        let content = resp.text().await?;
        let entity: Option<PostV1O11yReviewsError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent { status, content, entity }))
    }
}

/// AddAnnotationQueueItems enqueues traces, observations or sessions on a review queue. Each item names exactly one object, either by traceId / observationId / sessionId or by objectType plus objectId; every item enters PENDING. A queue id belonging to another org is a 404.
pub async fn post_v1_o11y_reviews_id_items(configuration: &configuration::Configuration, id: &str, add_items_in: models::AddItemsIn) -> Result<models::AnnItemsCreated, Error<PostV1O11yReviewsIdItemsError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_id = id;
    let p_add_items_in = add_items_in;

    let uri_str = format!("{}/v1/o11y/reviews/{id}/items", configuration.base_path, id=crate::apis::urlencode(p_id));
    let mut req_builder = configuration.client.request(reqwest::Method::POST, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }
    req_builder = req_builder.json(&p_add_items_in);

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();
    let content_type = resp
        .headers()
        .get("content-type")
        .and_then(|v| v.to_str().ok())
        .unwrap_or("application/octet-stream");
    let content_type = super::ContentType::from(content_type);

    if !status.is_client_error() && !status.is_server_error() {
        let content = resp.text().await?;
        match content_type {
            ContentType::Json => serde_json::from_str(&content).map_err(Error::from),
            ContentType::Text => return Err(Error::from(serde_json::Error::custom("Received `text/plain` content type response that cannot be converted to `models::AnnItemsCreated`"))),
            ContentType::Unsupported(unknown_type) => return Err(Error::from(serde_json::Error::custom(format!("Received `{unknown_type}` content type response that cannot be converted to `models::AnnItemsCreated`")))),
        }
    } else {
        let content = resp.text().await?;
        let entity: Option<PostV1O11yReviewsIdItemsError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent { status, content, entity }))
    }
}

/// Replaces one of the observability runtime's own objects — a dashboard, an alert rule, a saved view — in the runtime's own shapes, passed through unchanged. The fallthrough for the resources only the runtime knows.  A validated, org-scoped principal is required, and the write is confined to that principal's own tenant: the org is minted from its claim at ingress, a client copy never survives, and nothing in the request can widen the scope. Before the runtime is initialized, 503.
pub async fn put_v1_o11y_by_wildcard1(configuration: &configuration::Configuration, wildcard1: &str) -> Result<(), Error<PutV1O11yByWildcard1Error>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_wildcard1 = wildcard1;

    let uri_str = format!("{}/v1/o11y/{wildcard1}", configuration.base_path, wildcard1=crate::apis::urlencode(p_wildcard1));
    let mut req_builder = configuration.client.request(reqwest::Method::PUT, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();

    if !status.is_client_error() && !status.is_server_error() {
        Ok(())
    } else {
        let content = resp.text().await?;
        let entity: Option<PutV1O11yByWildcard1Error> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent { status, content, entity }))
    }
}

/// Published because this address accepts every method, but the runtime routes nothing here: the request reaches it as an unrouted path and no telemetry is read or written.
pub async fn trace_v1_o11y_by_wildcard1(configuration: &configuration::Configuration, wildcard1: &str) -> Result<(), Error<TraceV1O11yByWildcard1Error>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_wildcard1 = wildcard1;

    let uri_str = format!("{}/v1/o11y/{wildcard1}", configuration.base_path, wildcard1=crate::apis::urlencode(p_wildcard1));
    let mut req_builder = configuration.client.request(reqwest::Method::TRACE, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();

    if !status.is_client_error() && !status.is_server_error() {
        Ok(())
    } else {
        let content = resp.text().await?;
        let entity: Option<TraceV1O11yByWildcard1Error> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent { status, content, entity }))
    }
}

