/*
 * Hanzo Cloud API
 *
 * Composed from each subsystem's own projection of its router, in the fleet's mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 * Generated by: https://openapi-generator.tech
 */


use reqwest;
use serde::{Deserialize, Serialize, de::Error as _};
use crate::{apis::ResponseContent, models};
use super::{Error, configuration, ContentType};


/// struct for typed errors of method [`get_v1_audit`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum GetV1AuditError {
    UnknownValue(serde_json::Value),
}


/// List reads the caller's OWN org audit trail, newest first, with the total the filter matched so a console can page it.  Every filter is optional and applies WITHIN the caller's org — the org itself is the validated principal's and can never be widened by a request. Fails closed: an absent principal is a true \"not signed in\" (401), and a deployment with no local tamper-evident store answers an honest 501 rather than silently serving somebody else's trail.
pub async fn get_v1_audit(configuration: &configuration::Configuration, sub: Option<&str>, action: Option<&str>, resource: Option<&str>, resource_id: Option<&str>, result: Option<&str>, since: Option<&str>, until: Option<&str>, page_size: Option<&str>, p: Option<&str>) -> Result<models::TrailPage, Error<GetV1AuditError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_sub = sub;
    let p_action = action;
    let p_resource = resource;
    let p_resource_id = resource_id;
    let p_result = result;
    let p_since = since;
    let p_until = until;
    let p_page_size = page_size;
    let p_p = p;

    let uri_str = format!("{}/v1/audit", configuration.base_path);
    let mut req_builder = configuration.client.request(reqwest::Method::GET, &uri_str);

    if let Some(ref param_value) = p_sub {
        req_builder = req_builder.query(&[("sub", &param_value.to_string())]);
    }
    if let Some(ref param_value) = p_action {
        req_builder = req_builder.query(&[("action", &param_value.to_string())]);
    }
    if let Some(ref param_value) = p_resource {
        req_builder = req_builder.query(&[("resource", &param_value.to_string())]);
    }
    if let Some(ref param_value) = p_resource_id {
        req_builder = req_builder.query(&[("resourceId", &param_value.to_string())]);
    }
    if let Some(ref param_value) = p_result {
        req_builder = req_builder.query(&[("result", &param_value.to_string())]);
    }
    if let Some(ref param_value) = p_since {
        req_builder = req_builder.query(&[("since", &param_value.to_string())]);
    }
    if let Some(ref param_value) = p_until {
        req_builder = req_builder.query(&[("until", &param_value.to_string())]);
    }
    if let Some(ref param_value) = p_page_size {
        req_builder = req_builder.query(&[("pageSize", &param_value.to_string())]);
    }
    if let Some(ref param_value) = p_p {
        req_builder = req_builder.query(&[("p", &param_value.to_string())]);
    }
    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();
    let content_type = resp
        .headers()
        .get("content-type")
        .and_then(|v| v.to_str().ok())
        .unwrap_or("application/octet-stream");
    let content_type = super::ContentType::from(content_type);

    if !status.is_client_error() && !status.is_server_error() {
        let content = resp.text().await?;
        match content_type {
            ContentType::Json => serde_json::from_str(&content).map_err(Error::from),
            ContentType::Text => return Err(Error::from(serde_json::Error::custom("Received `text/plain` content type response that cannot be converted to `models::TrailPage`"))),
            ContentType::Unsupported(unknown_type) => return Err(Error::from(serde_json::Error::custom(format!("Received `{unknown_type}` content type response that cannot be converted to `models::TrailPage`")))),
        }
    } else {
        let content = resp.text().await?;
        let entity: Option<GetV1AuditError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent { status, content, entity }))
    }
}

