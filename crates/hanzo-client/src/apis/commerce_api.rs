/*
 * Hanzo Cloud API
 *
 * Composed from each subsystem's own projection of its router, in the fleet's mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 * Generated by: https://openapi-generator.tech
 */


use reqwest;
use serde::{Deserialize, Serialize, de::Error as _};
use crate::{apis::ResponseContent, models};
use super::{Error, configuration, ContentType};


/// struct for typed errors of method [`get_v1_commerce_admin_catalog`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum GetV1CommerceAdminCatalogError {
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`get_v1_commerce_catalog`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum GetV1CommerceCatalogError {
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`get_v1_commerce_currencies`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum GetV1CommerceCurrenciesError {
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`get_v1_commerce_deposits_by_id_status`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum GetV1CommerceDepositsByIdStatusError {
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`get_v1_commerce_tenant`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum GetV1CommerceTenantError {
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`get_v1_commerce_topup_rails`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum GetV1CommerceTopupRailsError {
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`post_v1_commerce_deposits`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum PostV1CommerceDepositsError {
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`post_v1_commerce_deposits_by_id_confirm`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum PostV1CommerceDepositsByIdConfirmError {
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`post_v1_commerce_topup_wallet`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum PostV1CommerceTopupWalletError {
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`post_v1_commerce_webhooks_by_provider`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum PostV1CommerceWebhooksByProviderError {
    UnknownValue(serde_json::Value),
}


/// Returns the brand-scoped catalog carrying the administrative economics the public projection withholds — upstream cost and margin percentage — for the margin surface the platform console administrates. The brand comes from the query and defaults to hanzo. PLATFORM admin only, enforced by the handler on top of the route's IAM gate: an ORG-level admin is refused 403 precisely so upstream cost and margin never reach a tenant.
pub async fn get_v1_commerce_admin_catalog(configuration: &configuration::Configuration, ) -> Result<(), Error<GetV1CommerceAdminCatalogError>> {

    let uri_str = format!("{}/v1/commerce/admin/catalog", configuration.base_path);
    let mut req_builder = configuration.client.request(reqwest::Method::GET, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();

    if !status.is_client_error() && !status.is_server_error() {
        Ok(())
    } else {
        let content = resp.text().await?;
        let entity: Option<GetV1CommerceAdminCatalogError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent { status, content, entity }))
    }
}

/// Returns the brand's published catalog — the shared source docs, the console sidebar and the pricing pages all read — with the brand taken from the query and defaulting to hanzo. It is public and cacheable, and it is the projection that deliberately omits cost and margin; those live only on the platform-admin projection.
pub async fn get_v1_commerce_catalog(configuration: &configuration::Configuration, ) -> Result<(), Error<GetV1CommerceCatalogError>> {

    let uri_str = format!("{}/v1/commerce/catalog", configuration.base_path);
    let mut req_builder = configuration.client.request(reqwest::Method::GET, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();

    if !status.is_client_error() && !status.is_server_error() {
        Ok(())
    } else {
        let content = resp.text().await?;
        let entity: Option<GetV1CommerceCatalogError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent { status, content, entity }))
    }
}

/// Returns every reference currency as one global list, so a store settings form or a product price picker binds real rows instead of a hardcoded array. It is a default-namespace read shared by every tenant rather than per-org data, and it is public and cacheable.
pub async fn get_v1_commerce_currencies(configuration: &configuration::Configuration, ) -> Result<(), Error<GetV1CommerceCurrenciesError>> {

    let uri_str = format!("{}/v1/commerce/currencies", configuration.base_path);
    let mut req_builder = configuration.client.request(reqwest::Method::GET, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();

    if !status.is_client_error() && !status.is_server_error() {
        Ok(())
    } else {
        let content = resp.text().await?;
        let entity: Option<GetV1CommerceCurrenciesError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent { status, content, entity }))
    }
}

/// Returns the deposit's state as the tenant's own backend reports it — pending, processing, settled or failed — which is what the checkout page polls until a terminal state or timeout. The tenant is resolved from the request host and the read is forwarded to that tenant's configured backend with the caller's Authorization carried through; without that header the answer is 401. An unknown host is 404, a tenant with no backend 503 and an unreachable backend 502.
pub async fn get_v1_commerce_deposits_by_id_status(configuration: &configuration::Configuration, id: &str) -> Result<(), Error<GetV1CommerceDepositsByIdStatusError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_id = id;

    let uri_str = format!("{}/v1/commerce/deposits/{id}/status", configuration.base_path, id=crate::apis::urlencode(p_id));
    let mut req_builder = configuration.client.request(reqwest::Method::GET, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();

    if !status.is_client_error() && !status.is_server_error() {
        Ok(())
    } else {
        let content = resp.text().await?;
        let entity: Option<GetV1CommerceDepositsByIdStatusError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent { status, content, entity }))
    }
}

/// Answers the branding, identity issuer and client id, identity-verification config, enabled payment providers, return-URL allowlist and public payment application config for the tenant the request HOST resolves to. It is genuinely public and unauthenticated — a checkout page calls it before anyone has signed in — and it carries the same public payment config the authenticated config read does, so the card iframe can never initialize against a different application than the one that will be charged. Only ENABLED providers are listed and no credential path is ever projected. An unresolvable host answers a constant 404 that does not echo the host, so the endpoint cannot be used to enumerate tenants; a successful answer is cacheable for a minute.
pub async fn get_v1_commerce_tenant(configuration: &configuration::Configuration, ) -> Result<(), Error<GetV1CommerceTenantError>> {

    let uri_str = format!("{}/v1/commerce/tenant", configuration.base_path);
    let mut req_builder = configuration.client.request(reqwest::Method::GET, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();

    if !status.is_client_error() && !status.is_server_error() {
        Ok(())
    } else {
        let content = resp.text().await?;
        let entity: Option<GetV1CommerceTenantError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent { status, content, entity }))
    }
}

/// TopupRails lists the accepted (chain, token, treasury) triples, so a browser can render \"send USDC here\" without the addresses being baked into its bundle.  This exists because the console previously gated its top-up UI on NEXT_PUBLIC_HANZO_HUSD_ADDRESS/_TREASURY — build-time constants. Enabling a rail therefore meant rebuilding and redeploying the frontend, and with them unset the UI reported \"not available yet\" no matter what the server could actually accept. Serving the set at runtime keeps ONE source of truth (the server's config) and lets a rail be switched on without shipping a bundle.  Everything here is public on-chain data; no secret is exposed, and the set is empty on a deployment that accepts no crypto rail.
pub async fn get_v1_commerce_topup_rails(configuration: &configuration::Configuration, ) -> Result<models::RailList, Error<GetV1CommerceTopupRailsError>> {

    let uri_str = format!("{}/v1/commerce/topup/rails", configuration.base_path);
    let mut req_builder = configuration.client.request(reqwest::Method::GET, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();
    let content_type = resp
        .headers()
        .get("content-type")
        .and_then(|v| v.to_str().ok())
        .unwrap_or("application/octet-stream");
    let content_type = super::ContentType::from(content_type);

    if !status.is_client_error() && !status.is_server_error() {
        let content = resp.text().await?;
        match content_type {
            ContentType::Json => serde_json::from_str(&content).map_err(Error::from),
            ContentType::Text => return Err(Error::from(serde_json::Error::custom("Received `text/plain` content type response that cannot be converted to `models::RailList`"))),
            ContentType::Unsupported(unknown_type) => return Err(Error::from(serde_json::Error::custom(format!("Received `{unknown_type}` content type response that cannot be converted to `models::RailList`")))),
        }
    } else {
        let content = resp.text().await?;
        let entity: Option<GetV1CommerceTopupRailsError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent { status, content, entity }))
    }
}

/// Forwards the deposit request to the backend belonging to the tenant the request HOST resolves to, and answers that backend's status and body verbatim. The upstream is always taken from the resolved tenant's configured backend and never from the Host header itself, so a spoofed host cannot redirect the forward; only the Authorization and Content-Type headers are carried forward, so cookies and custom headers never leak to the backend. An unknown host is 404 with no echo of the host, a caller with no Authorization is 401, a tenant with no backend configured is 503, and an unreachable backend is 502.
pub async fn post_v1_commerce_deposits(configuration: &configuration::Configuration, ) -> Result<(), Error<PostV1CommerceDepositsError>> {

    let uri_str = format!("{}/v1/commerce/deposits", configuration.base_path);
    let mut req_builder = configuration.client.request(reqwest::Method::POST, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();

    if !status.is_client_error() && !status.is_server_error() {
        Ok(())
    } else {
        let content = resp.text().await?;
        let entity: Option<PostV1CommerceDepositsError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent { status, content, entity }))
    }
}

/// Forwards the confirmation for the addressed deposit to the resolved tenant's own backend and answers that backend's reply verbatim. The tenant comes from the request host, the upstream from that tenant's configured backend, and an Authorization header is required — the confirmation is the caller's, and this service only relays it. An unknown host is 404, a missing deposit id 400, a tenant with no backend configured 503 and an unreachable backend 502.
pub async fn post_v1_commerce_deposits_by_id_confirm(configuration: &configuration::Configuration, id: &str) -> Result<(), Error<PostV1CommerceDepositsByIdConfirmError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_id = id;

    let uri_str = format!("{}/v1/commerce/deposits/{id}/confirm", configuration.base_path, id=crate::apis::urlencode(p_id));
    let mut req_builder = configuration.client.request(reqwest::Method::POST, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();

    if !status.is_client_error() && !status.is_server_error() {
        Ok(())
    } else {
        let content = resp.text().await?;
        let entity: Option<PostV1CommerceDepositsByIdConfirmError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent { status, content, entity }))
    }
}

/// WalletTopup credits the caller's org for a stablecoin transfer they already sent to the treasury. It reads the receipt from that rail's chain, confirms a mined, successful ERC-20 Transfer to the rail's treasury, derives USD cents from the on-chain value using the token's own decimals, records the credit, and returns the amount plus the new balance.  The credited amount is the ON-CHAIN value, never a number the caller sends, and the credit lands on the caller's own validated org — there is no way to name a third-party subject. Nothing is credited that the chain did not confirm: a missing, failed or non-matching transaction is refused, and a deployment with no payment rail enabled says so rather than inventing a credit.
pub async fn post_v1_commerce_topup_wallet(configuration: &configuration::Configuration, wallet_topup_req: models::WalletTopupReq) -> Result<models::WalletTopupResp, Error<PostV1CommerceTopupWalletError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_wallet_topup_req = wallet_topup_req;

    let uri_str = format!("{}/v1/commerce/topup/wallet", configuration.base_path);
    let mut req_builder = configuration.client.request(reqwest::Method::POST, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }
    req_builder = req_builder.json(&p_wallet_topup_req);

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();
    let content_type = resp
        .headers()
        .get("content-type")
        .and_then(|v| v.to_str().ok())
        .unwrap_or("application/octet-stream");
    let content_type = super::ContentType::from(content_type);

    if !status.is_client_error() && !status.is_server_error() {
        let content = resp.text().await?;
        match content_type {
            ContentType::Json => serde_json::from_str(&content).map_err(Error::from),
            ContentType::Text => return Err(Error::from(serde_json::Error::custom("Received `text/plain` content type response that cannot be converted to `models::WalletTopupResp`"))),
            ContentType::Unsupported(unknown_type) => return Err(Error::from(serde_json::Error::custom(format!("Received `{unknown_type}` content type response that cannot be converted to `models::WalletTopupResp`")))),
        }
    } else {
        let content = resp.text().await?;
        let entity: Option<PostV1CommerceTopupWalletError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent { status, content, entity }))
    }
}

/// Forwards the provider's event — body and original signature headers intact — to the backend of the tenant the request host resolves to, so that backend can verify it with its own tenant-scoped signing key. Commerce deliberately does NOT verify the signature here: the keys live with the tenant backend, and holding a second copy would be a second place to rotate and a stale cache that rejects live webhooks. A provider outside the known set is 404, as is an unresolvable host; a tenant with no backend configured is 503.
pub async fn post_v1_commerce_webhooks_by_provider(configuration: &configuration::Configuration, provider: &str) -> Result<(), Error<PostV1CommerceWebhooksByProviderError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_provider = provider;

    let uri_str = format!("{}/v1/commerce/webhooks/{provider}", configuration.base_path, provider=crate::apis::urlencode(p_provider));
    let mut req_builder = configuration.client.request(reqwest::Method::POST, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();

    if !status.is_client_error() && !status.is_server_error() {
        Ok(())
    } else {
        let content = resp.text().await?;
        let entity: Option<PostV1CommerceWebhooksByProviderError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent { status, content, entity }))
    }
}

