/*
 * Hanzo Cloud API
 *
 * Composed from each subsystem's own projection of its router, in the fleet's mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 * Generated by: https://openapi-generator.tech
 */


use reqwest;
use serde::{Deserialize, Serialize, de::Error as _};
use crate::{apis::ResponseContent, models};
use super::{Error, configuration, ContentType};


/// struct for typed errors of method [`delete_v1_pubsub_jetstream_streams_stream`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum DeleteV1PubsubJetstreamStreamsStreamError {
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`delete_v1_pubsub_jetstream_streams_stream_consumers_name`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum DeleteV1PubsubJetstreamStreamsStreamConsumersNameError {
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`delete_v1_pubsub_kv_bucket`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum DeleteV1PubsubKvBucketError {
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`delete_v1_pubsub_kv_bucket_key`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum DeleteV1PubsubKvBucketKeyError {
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`get_v1_pubsub_jetstream_streams`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum GetV1PubsubJetstreamStreamsError {
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`get_v1_pubsub_jetstream_streams_stream`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum GetV1PubsubJetstreamStreamsStreamError {
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`get_v1_pubsub_jetstream_streams_stream_consumers`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum GetV1PubsubJetstreamStreamsStreamConsumersError {
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`get_v1_pubsub_jetstream_streams_stream_consumers_name`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum GetV1PubsubJetstreamStreamsStreamConsumersNameError {
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`get_v1_pubsub_kv_bucket_key`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum GetV1PubsubKvBucketKeyError {
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`get_v1_pubsub_kv_bucket_key_history`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum GetV1PubsubKvBucketKeyHistoryError {
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`post_v1_pubsub_jetstream_streams`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum PostV1PubsubJetstreamStreamsError {
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`post_v1_pubsub_jetstream_streams_stream_consumers`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum PostV1PubsubJetstreamStreamsStreamConsumersError {
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`post_v1_pubsub_jetstream_streams_stream_consumers_name_next`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum PostV1PubsubJetstreamStreamsStreamConsumersNameNextError {
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`post_v1_pubsub_kv_bucket`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum PostV1PubsubKvBucketError {
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`post_v1_pubsub_publish`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum PostV1PubsubPublishError {
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`post_v1_pubsub_request`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum PostV1PubsubRequestError {
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`put_v1_pubsub_jetstream_streams_stream`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum PutV1PubsubJetstreamStreamsStreamError {
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`put_v1_pubsub_kv_bucket_key`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum PutV1PubsubKvBucketKeyError {
    UnknownValue(serde_json::Value),
}


/// DeleteStream removes one stream of the caller's org — its retained messages and its consumers with it — and answers 204 with no body. 404 when the org has no stream of that name.
pub async fn delete_v1_pubsub_jetstream_streams_stream(configuration: &configuration::Configuration, stream: &str) -> Result<(), Error<DeleteV1PubsubJetstreamStreamsStreamError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_stream = stream;

    let uri_str = format!("{}/v1/pubsub/jetstream/streams/{stream}", configuration.base_path, stream=crate::apis::urlencode(p_stream));
    let mut req_builder = configuration.client.request(reqwest::Method::DELETE, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();

    if !status.is_client_error() && !status.is_server_error() {
        Ok(())
    } else {
        let content = resp.text().await?;
        let entity: Option<DeleteV1PubsubJetstreamStreamsStreamError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent { status, content, entity }))
    }
}

/// DeleteConsumer removes one consumer — its cursor, not the stream's messages — and answers 204 with no body. 404 when the stream or the consumer does not exist.
pub async fn delete_v1_pubsub_jetstream_streams_stream_consumers_name(configuration: &configuration::Configuration, stream: &str, name: &str) -> Result<(), Error<DeleteV1PubsubJetstreamStreamsStreamConsumersNameError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_stream = stream;
    let p_name = name;

    let uri_str = format!("{}/v1/pubsub/jetstream/streams/{stream}/consumers/{name}", configuration.base_path, stream=crate::apis::urlencode(p_stream), name=crate::apis::urlencode(p_name));
    let mut req_builder = configuration.client.request(reqwest::Method::DELETE, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();

    if !status.is_client_error() && !status.is_server_error() {
        Ok(())
    } else {
        let content = resp.text().await?;
        let entity: Option<DeleteV1PubsubJetstreamStreamsStreamConsumersNameError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent { status, content, entity }))
    }
}

/// DeleteBucket removes one bucket of the caller's org — every key and every revision with it — and answers 204 with no body. 404 when the org has no bucket of that name.
pub async fn delete_v1_pubsub_kv_bucket(configuration: &configuration::Configuration, bucket: &str) -> Result<(), Error<DeleteV1PubsubKvBucketError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_bucket = bucket;

    let uri_str = format!("{}/v1/pubsub/kv/{bucket}", configuration.base_path, bucket=crate::apis::urlencode(p_bucket));
    let mut req_builder = configuration.client.request(reqwest::Method::DELETE, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();

    if !status.is_client_error() && !status.is_server_error() {
        Ok(())
    } else {
        let content = resp.text().await?;
        let entity: Option<DeleteV1PubsubKvBucketError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent { status, content, entity }))
    }
}

/// Delete removes one key — a delete marker in the key's history, so watchers see it and Get answers 404 — and answers 204 with no body. 404 when the bucket does not exist.
pub async fn delete_v1_pubsub_kv_bucket_key(configuration: &configuration::Configuration, bucket: &str, key: &str) -> Result<(), Error<DeleteV1PubsubKvBucketKeyError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_bucket = bucket;
    let p_key = key;

    let uri_str = format!("{}/v1/pubsub/kv/{bucket}/{key}", configuration.base_path, bucket=crate::apis::urlencode(p_bucket), key=crate::apis::urlencode(p_key));
    let mut req_builder = configuration.client.request(reqwest::Method::DELETE, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();

    if !status.is_client_error() && !status.is_server_error() {
        Ok(())
    } else {
        let content = resp.text().await?;
        let entity: Option<DeleteV1PubsubKvBucketKeyError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent { status, content, entity }))
    }
}

/// ListStreams returns the org's streams, sorted by name.  A stream is the durable log: it captures every message published to its subjects and retains them by its own limits, independent of any consumer. The listing is org-scoped server-side — one org can never see another's streams, and the platform's own planes never appear.
pub async fn get_v1_pubsub_jetstream_streams(configuration: &configuration::Configuration, ) -> Result<models::StreamPage, Error<GetV1PubsubJetstreamStreamsError>> {

    let uri_str = format!("{}/v1/pubsub/jetstream/streams", configuration.base_path);
    let mut req_builder = configuration.client.request(reqwest::Method::GET, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();
    let content_type = resp
        .headers()
        .get("content-type")
        .and_then(|v| v.to_str().ok())
        .unwrap_or("application/octet-stream");
    let content_type = super::ContentType::from(content_type);

    if !status.is_client_error() && !status.is_server_error() {
        let content = resp.text().await?;
        match content_type {
            ContentType::Json => serde_json::from_str(&content).map_err(Error::from),
            ContentType::Text => return Err(Error::from(serde_json::Error::custom("Received `text/plain` content type response that cannot be converted to `models::StreamPage`"))),
            ContentType::Unsupported(unknown_type) => return Err(Error::from(serde_json::Error::custom(format!("Received `{unknown_type}` content type response that cannot be converted to `models::StreamPage`")))),
        }
    } else {
        let content = resp.text().await?;
        let entity: Option<GetV1PubsubJetstreamStreamsError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent { status, content, entity }))
    }
}

/// GetStream returns one stream of the caller's org — its configuration and its live state (messages, bytes, sequence range, consumer count). 404 when the org has no stream of that name.
pub async fn get_v1_pubsub_jetstream_streams_stream(configuration: &configuration::Configuration, stream: &str) -> Result<models::StreamRecord, Error<GetV1PubsubJetstreamStreamsStreamError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_stream = stream;

    let uri_str = format!("{}/v1/pubsub/jetstream/streams/{stream}", configuration.base_path, stream=crate::apis::urlencode(p_stream));
    let mut req_builder = configuration.client.request(reqwest::Method::GET, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();
    let content_type = resp
        .headers()
        .get("content-type")
        .and_then(|v| v.to_str().ok())
        .unwrap_or("application/octet-stream");
    let content_type = super::ContentType::from(content_type);

    if !status.is_client_error() && !status.is_server_error() {
        let content = resp.text().await?;
        match content_type {
            ContentType::Json => serde_json::from_str(&content).map_err(Error::from),
            ContentType::Text => return Err(Error::from(serde_json::Error::custom("Received `text/plain` content type response that cannot be converted to `models::StreamRecord`"))),
            ContentType::Unsupported(unknown_type) => return Err(Error::from(serde_json::Error::custom(format!("Received `{unknown_type}` content type response that cannot be converted to `models::StreamRecord`")))),
        }
    } else {
        let content = resp.text().await?;
        let entity: Option<GetV1PubsubJetstreamStreamsStreamError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent { status, content, entity }))
    }
}

/// ListConsumers returns one stream's consumers, sorted by name. 404 when the org has no stream of that name.
pub async fn get_v1_pubsub_jetstream_streams_stream_consumers(configuration: &configuration::Configuration, stream: &str) -> Result<models::ConsumerPage, Error<GetV1PubsubJetstreamStreamsStreamConsumersError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_stream = stream;

    let uri_str = format!("{}/v1/pubsub/jetstream/streams/{stream}/consumers", configuration.base_path, stream=crate::apis::urlencode(p_stream));
    let mut req_builder = configuration.client.request(reqwest::Method::GET, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();
    let content_type = resp
        .headers()
        .get("content-type")
        .and_then(|v| v.to_str().ok())
        .unwrap_or("application/octet-stream");
    let content_type = super::ContentType::from(content_type);

    if !status.is_client_error() && !status.is_server_error() {
        let content = resp.text().await?;
        match content_type {
            ContentType::Json => serde_json::from_str(&content).map_err(Error::from),
            ContentType::Text => return Err(Error::from(serde_json::Error::custom("Received `text/plain` content type response that cannot be converted to `models::ConsumerPage`"))),
            ContentType::Unsupported(unknown_type) => return Err(Error::from(serde_json::Error::custom(format!("Received `{unknown_type}` content type response that cannot be converted to `models::ConsumerPage`")))),
        }
    } else {
        let content = resp.text().await?;
        let entity: Option<GetV1PubsubJetstreamStreamsStreamConsumersError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent { status, content, entity }))
    }
}

/// GetConsumer returns one consumer of one org stream — its configuration and its cursor: delivered and acknowledged sequences, pending and redelivered counts. 404 when the stream or the consumer does not exist.
pub async fn get_v1_pubsub_jetstream_streams_stream_consumers_name(configuration: &configuration::Configuration, stream: &str, name: &str) -> Result<models::ConsumerRecord, Error<GetV1PubsubJetstreamStreamsStreamConsumersNameError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_stream = stream;
    let p_name = name;

    let uri_str = format!("{}/v1/pubsub/jetstream/streams/{stream}/consumers/{name}", configuration.base_path, stream=crate::apis::urlencode(p_stream), name=crate::apis::urlencode(p_name));
    let mut req_builder = configuration.client.request(reqwest::Method::GET, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();
    let content_type = resp
        .headers()
        .get("content-type")
        .and_then(|v| v.to_str().ok())
        .unwrap_or("application/octet-stream");
    let content_type = super::ContentType::from(content_type);

    if !status.is_client_error() && !status.is_server_error() {
        let content = resp.text().await?;
        match content_type {
            ContentType::Json => serde_json::from_str(&content).map_err(Error::from),
            ContentType::Text => return Err(Error::from(serde_json::Error::custom("Received `text/plain` content type response that cannot be converted to `models::ConsumerRecord`"))),
            ContentType::Unsupported(unknown_type) => return Err(Error::from(serde_json::Error::custom(format!("Received `{unknown_type}` content type response that cannot be converted to `models::ConsumerRecord`")))),
        }
    } else {
        let content = resp.text().await?;
        let entity: Option<GetV1PubsubJetstreamStreamsStreamConsumersNameError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent { status, content, entity }))
    }
}

/// Get returns one key's current value and revision. 404 when the bucket does not exist, the key was never written, or its latest revision is a delete.
pub async fn get_v1_pubsub_kv_bucket_key(configuration: &configuration::Configuration, bucket: &str, key: &str) -> Result<models::KvEntry, Error<GetV1PubsubKvBucketKeyError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_bucket = bucket;
    let p_key = key;

    let uri_str = format!("{}/v1/pubsub/kv/{bucket}/{key}", configuration.base_path, bucket=crate::apis::urlencode(p_bucket), key=crate::apis::urlencode(p_key));
    let mut req_builder = configuration.client.request(reqwest::Method::GET, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();
    let content_type = resp
        .headers()
        .get("content-type")
        .and_then(|v| v.to_str().ok())
        .unwrap_or("application/octet-stream");
    let content_type = super::ContentType::from(content_type);

    if !status.is_client_error() && !status.is_server_error() {
        let content = resp.text().await?;
        match content_type {
            ContentType::Json => serde_json::from_str(&content).map_err(Error::from),
            ContentType::Text => return Err(Error::from(serde_json::Error::custom("Received `text/plain` content type response that cannot be converted to `models::KvEntry`"))),
            ContentType::Unsupported(unknown_type) => return Err(Error::from(serde_json::Error::custom(format!("Received `{unknown_type}` content type response that cannot be converted to `models::KvEntry`")))),
        }
    } else {
        let content = resp.text().await?;
        let entity: Option<GetV1PubsubKvBucketKeyError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent { status, content, entity }))
    }
}

/// History returns one key's retained revisions, oldest first — every put and every delete marker up to the bucket's History depth. 404 when the bucket does not exist or the key was never written.
pub async fn get_v1_pubsub_kv_bucket_key_history(configuration: &configuration::Configuration, bucket: &str, key: &str) -> Result<models::KvPage, Error<GetV1PubsubKvBucketKeyHistoryError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_bucket = bucket;
    let p_key = key;

    let uri_str = format!("{}/v1/pubsub/kv/{bucket}/{key}/history", configuration.base_path, bucket=crate::apis::urlencode(p_bucket), key=crate::apis::urlencode(p_key));
    let mut req_builder = configuration.client.request(reqwest::Method::GET, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();
    let content_type = resp
        .headers()
        .get("content-type")
        .and_then(|v| v.to_str().ok())
        .unwrap_or("application/octet-stream");
    let content_type = super::ContentType::from(content_type);

    if !status.is_client_error() && !status.is_server_error() {
        let content = resp.text().await?;
        match content_type {
            ContentType::Json => serde_json::from_str(&content).map_err(Error::from),
            ContentType::Text => return Err(Error::from(serde_json::Error::custom("Received `text/plain` content type response that cannot be converted to `models::KvPage`"))),
            ContentType::Unsupported(unknown_type) => return Err(Error::from(serde_json::Error::custom(format!("Received `{unknown_type}` content type response that cannot be converted to `models::KvPage`")))),
        }
    } else {
        let content = resp.text().await?;
        let entity: Option<GetV1PubsubKvBucketKeyHistoryError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent { status, content, entity }))
    }
}

/// CreateStream creates a durable stream capturing the given subjects and returns it. 409 when the org already has a stream of that name; the subjects are the org's own and cannot collide with another org's.
pub async fn post_v1_pubsub_jetstream_streams(configuration: &configuration::Configuration, stream_write: models::StreamWrite) -> Result<models::StreamRecord, Error<PostV1PubsubJetstreamStreamsError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_stream_write = stream_write;

    let uri_str = format!("{}/v1/pubsub/jetstream/streams", configuration.base_path);
    let mut req_builder = configuration.client.request(reqwest::Method::POST, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }
    req_builder = req_builder.json(&p_stream_write);

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();
    let content_type = resp
        .headers()
        .get("content-type")
        .and_then(|v| v.to_str().ok())
        .unwrap_or("application/octet-stream");
    let content_type = super::ContentType::from(content_type);

    if !status.is_client_error() && !status.is_server_error() {
        let content = resp.text().await?;
        match content_type {
            ContentType::Json => serde_json::from_str(&content).map_err(Error::from),
            ContentType::Text => return Err(Error::from(serde_json::Error::custom("Received `text/plain` content type response that cannot be converted to `models::StreamRecord`"))),
            ContentType::Unsupported(unknown_type) => return Err(Error::from(serde_json::Error::custom(format!("Received `{unknown_type}` content type response that cannot be converted to `models::StreamRecord`")))),
        }
    } else {
        let content = resp.text().await?;
        let entity: Option<PostV1PubsubJetstreamStreamsError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent { status, content, entity }))
    }
}

/// CreateConsumer creates a durable consumer on one stream and returns it. A consumer is a named cursor: it tracks what has been delivered and what is acknowledged, so many workers can share it and none sees a message twice outside redelivery. 409 when the stream already has a consumer of that name with a different configuration.
pub async fn post_v1_pubsub_jetstream_streams_stream_consumers(configuration: &configuration::Configuration, stream: &str, consumer_write: models::ConsumerWrite) -> Result<models::ConsumerRecord, Error<PostV1PubsubJetstreamStreamsStreamConsumersError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_stream = stream;
    let p_consumer_write = consumer_write;

    let uri_str = format!("{}/v1/pubsub/jetstream/streams/{stream}/consumers", configuration.base_path, stream=crate::apis::urlencode(p_stream));
    let mut req_builder = configuration.client.request(reqwest::Method::POST, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }
    req_builder = req_builder.json(&p_consumer_write);

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();
    let content_type = resp
        .headers()
        .get("content-type")
        .and_then(|v| v.to_str().ok())
        .unwrap_or("application/octet-stream");
    let content_type = super::ContentType::from(content_type);

    if !status.is_client_error() && !status.is_server_error() {
        let content = resp.text().await?;
        match content_type {
            ContentType::Json => serde_json::from_str(&content).map_err(Error::from),
            ContentType::Text => return Err(Error::from(serde_json::Error::custom("Received `text/plain` content type response that cannot be converted to `models::ConsumerRecord`"))),
            ContentType::Unsupported(unknown_type) => return Err(Error::from(serde_json::Error::custom(format!("Received `{unknown_type}` content type response that cannot be converted to `models::ConsumerRecord`")))),
        }
    } else {
        let content = resp.text().await?;
        let entity: Option<PostV1PubsubJetstreamStreamsStreamConsumersError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent { status, content, entity }))
    }
}

/// Fetch pulls the next batch from a consumer and acknowledges it — the request/response way to consume a stream. The hand-off is at-most-once: a message returned here is acked here, so a caller that loses the response does not see it again. Workers needing at-least-once delivery consume the same consumer over the NATS port, where acks are theirs to send. An empty batch after the wait is an empty page, not an error.
pub async fn post_v1_pubsub_jetstream_streams_stream_consumers_name_next(configuration: &configuration::Configuration, stream: &str, name: &str, fetch_query: models::FetchQuery) -> Result<models::MessagePage, Error<PostV1PubsubJetstreamStreamsStreamConsumersNameNextError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_stream = stream;
    let p_name = name;
    let p_fetch_query = fetch_query;

    let uri_str = format!("{}/v1/pubsub/jetstream/streams/{stream}/consumers/{name}/next", configuration.base_path, stream=crate::apis::urlencode(p_stream), name=crate::apis::urlencode(p_name));
    let mut req_builder = configuration.client.request(reqwest::Method::POST, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }
    req_builder = req_builder.json(&p_fetch_query);

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();
    let content_type = resp
        .headers()
        .get("content-type")
        .and_then(|v| v.to_str().ok())
        .unwrap_or("application/octet-stream");
    let content_type = super::ContentType::from(content_type);

    if !status.is_client_error() && !status.is_server_error() {
        let content = resp.text().await?;
        match content_type {
            ContentType::Json => serde_json::from_str(&content).map_err(Error::from),
            ContentType::Text => return Err(Error::from(serde_json::Error::custom("Received `text/plain` content type response that cannot be converted to `models::MessagePage`"))),
            ContentType::Unsupported(unknown_type) => return Err(Error::from(serde_json::Error::custom(format!("Received `{unknown_type}` content type response that cannot be converted to `models::MessagePage`")))),
        }
    } else {
        let content = resp.text().await?;
        let entity: Option<PostV1PubsubJetstreamStreamsStreamConsumersNameNextError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent { status, content, entity }))
    }
}

/// CreateBucket creates a KV bucket and returns it. A bucket is keyed state on the same durable plane as the streams: each key holds up to History revisions, entries can expire by TTL, and watchers on the NATS port see every write. 409 when the org already has a bucket of that name.
pub async fn post_v1_pubsub_kv_bucket(configuration: &configuration::Configuration, bucket: &str, bucket_write: models::BucketWrite) -> Result<models::BucketRecord, Error<PostV1PubsubKvBucketError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_bucket = bucket;
    let p_bucket_write = bucket_write;

    let uri_str = format!("{}/v1/pubsub/kv/{bucket}", configuration.base_path, bucket=crate::apis::urlencode(p_bucket));
    let mut req_builder = configuration.client.request(reqwest::Method::POST, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }
    req_builder = req_builder.json(&p_bucket_write);

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();
    let content_type = resp
        .headers()
        .get("content-type")
        .and_then(|v| v.to_str().ok())
        .unwrap_or("application/octet-stream");
    let content_type = super::ContentType::from(content_type);

    if !status.is_client_error() && !status.is_server_error() {
        let content = resp.text().await?;
        match content_type {
            ContentType::Json => serde_json::from_str(&content).map_err(Error::from),
            ContentType::Text => return Err(Error::from(serde_json::Error::custom("Received `text/plain` content type response that cannot be converted to `models::BucketRecord`"))),
            ContentType::Unsupported(unknown_type) => return Err(Error::from(serde_json::Error::custom(format!("Received `{unknown_type}` content type response that cannot be converted to `models::BucketRecord`")))),
        }
    } else {
        let content = resp.text().await?;
        let entity: Option<PostV1PubsubKvBucketError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent { status, content, entity }))
    }
}

/// Publish puts one message on the org's bus. When a stream captures the subject the write is DURABLE — the receipt names the stream and sequence only after JetStream has it on storage, and a repeated Nats-Msg-Id header within the dedup window answers duplicate instead of storing twice. When nothing captures it, the message goes out core NATS: delivered to current subscribers, receipt {ok}, nothing retained.
pub async fn post_v1_pubsub_publish(configuration: &configuration::Configuration, bus_publish: models::BusPublish) -> Result<models::BusAck, Error<PostV1PubsubPublishError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_bus_publish = bus_publish;

    let uri_str = format!("{}/v1/pubsub/publish", configuration.base_path);
    let mut req_builder = configuration.client.request(reqwest::Method::POST, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }
    req_builder = req_builder.json(&p_bus_publish);

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();
    let content_type = resp
        .headers()
        .get("content-type")
        .and_then(|v| v.to_str().ok())
        .unwrap_or("application/octet-stream");
    let content_type = super::ContentType::from(content_type);

    if !status.is_client_error() && !status.is_server_error() {
        let content = resp.text().await?;
        match content_type {
            ContentType::Json => serde_json::from_str(&content).map_err(Error::from),
            ContentType::Text => return Err(Error::from(serde_json::Error::custom("Received `text/plain` content type response that cannot be converted to `models::BusAck`"))),
            ContentType::Unsupported(unknown_type) => return Err(Error::from(serde_json::Error::custom(format!("Received `{unknown_type}` content type response that cannot be converted to `models::BusAck`")))),
        }
    } else {
        let content = resp.text().await?;
        let entity: Option<PostV1PubsubPublishError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent { status, content, entity }))
    }
}

/// Request sends one request on the org's bus and waits for one reply — the synchronous half of pub/sub, for callers speaking to a responder subscribed on the NATS port. 404 when nobody is listening on the subject; 408 when a responder exists but no reply arrived within the timeout.
pub async fn post_v1_pubsub_request(configuration: &configuration::Configuration, bus_request: models::BusRequest) -> Result<models::BusMessage, Error<PostV1PubsubRequestError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_bus_request = bus_request;

    let uri_str = format!("{}/v1/pubsub/request", configuration.base_path);
    let mut req_builder = configuration.client.request(reqwest::Method::POST, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }
    req_builder = req_builder.json(&p_bus_request);

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();
    let content_type = resp
        .headers()
        .get("content-type")
        .and_then(|v| v.to_str().ok())
        .unwrap_or("application/octet-stream");
    let content_type = super::ContentType::from(content_type);

    if !status.is_client_error() && !status.is_server_error() {
        let content = resp.text().await?;
        match content_type {
            ContentType::Json => serde_json::from_str(&content).map_err(Error::from),
            ContentType::Text => return Err(Error::from(serde_json::Error::custom("Received `text/plain` content type response that cannot be converted to `models::BusMessage`"))),
            ContentType::Unsupported(unknown_type) => return Err(Error::from(serde_json::Error::custom(format!("Received `{unknown_type}` content type response that cannot be converted to `models::BusMessage`")))),
        }
    } else {
        let content = resp.text().await?;
        let entity: Option<PostV1PubsubRequestError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent { status, content, entity }))
    }
}

/// UpdateStream rewrites a stream's configuration — subjects, limits, discard — and returns the updated stream. It is a PUT: the spec sent replaces the spec held, with one reading for the enums a caller omits — an empty storage, retention or discard keeps the stream's current one, because JetStream holds storage and retention immutable and refuses a change with a 400 rather than this door pretending it took.
pub async fn put_v1_pubsub_jetstream_streams_stream(configuration: &configuration::Configuration, stream: &str, stream_update: models::StreamUpdate) -> Result<models::StreamRecord, Error<PutV1PubsubJetstreamStreamsStreamError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_stream = stream;
    let p_stream_update = stream_update;

    let uri_str = format!("{}/v1/pubsub/jetstream/streams/{stream}", configuration.base_path, stream=crate::apis::urlencode(p_stream));
    let mut req_builder = configuration.client.request(reqwest::Method::PUT, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }
    req_builder = req_builder.json(&p_stream_update);

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();
    let content_type = resp
        .headers()
        .get("content-type")
        .and_then(|v| v.to_str().ok())
        .unwrap_or("application/octet-stream");
    let content_type = super::ContentType::from(content_type);

    if !status.is_client_error() && !status.is_server_error() {
        let content = resp.text().await?;
        match content_type {
            ContentType::Json => serde_json::from_str(&content).map_err(Error::from),
            ContentType::Text => return Err(Error::from(serde_json::Error::custom("Received `text/plain` content type response that cannot be converted to `models::StreamRecord`"))),
            ContentType::Unsupported(unknown_type) => return Err(Error::from(serde_json::Error::custom(format!("Received `{unknown_type}` content type response that cannot be converted to `models::StreamRecord`")))),
        }
    } else {
        let content = resp.text().await?;
        let entity: Option<PutV1PubsubJetstreamStreamsStreamError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent { status, content, entity }))
    }
}

/// Put sets one key to one value and returns the revision the write created. Writes are versioned: each put is a new revision and the bucket retains up to its History of them per key.
pub async fn put_v1_pubsub_kv_bucket_key(configuration: &configuration::Configuration, bucket: &str, key: &str, kv_write: models::KvWrite) -> Result<models::KvAck, Error<PutV1PubsubKvBucketKeyError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_bucket = bucket;
    let p_key = key;
    let p_kv_write = kv_write;

    let uri_str = format!("{}/v1/pubsub/kv/{bucket}/{key}", configuration.base_path, bucket=crate::apis::urlencode(p_bucket), key=crate::apis::urlencode(p_key));
    let mut req_builder = configuration.client.request(reqwest::Method::PUT, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }
    req_builder = req_builder.json(&p_kv_write);

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();
    let content_type = resp
        .headers()
        .get("content-type")
        .and_then(|v| v.to_str().ok())
        .unwrap_or("application/octet-stream");
    let content_type = super::ContentType::from(content_type);

    if !status.is_client_error() && !status.is_server_error() {
        let content = resp.text().await?;
        match content_type {
            ContentType::Json => serde_json::from_str(&content).map_err(Error::from),
            ContentType::Text => return Err(Error::from(serde_json::Error::custom("Received `text/plain` content type response that cannot be converted to `models::KvAck`"))),
            ContentType::Unsupported(unknown_type) => return Err(Error::from(serde_json::Error::custom(format!("Received `{unknown_type}` content type response that cannot be converted to `models::KvAck`")))),
        }
    } else {
        let content = resp.text().await?;
        let entity: Option<PutV1PubsubKvBucketKeyError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent { status, content, entity }))
    }
}

