/*
 * Hanzo Cloud — Unified API (V8 · Open Edition)
 *
 * The single unified OpenAPI surface for ALL Hanzo services, aggregated from the per-service specs. Every route is https://api.hanzo.ai/v1/<service>/_*. Grouped by the canonical domains in capabilities.yaml (the ONE registry). cloud/openapi.yaml is hanzoai/cloud's own woven document, copied verbatim by sync.py, and it wins wherever it and a hand-written spec describe the same route. Regenerated by merge.py — edit the per-service <svc>/openapi.yaml + capabilities.yaml, not this file.
 *
 * The version of the OpenAPI document: 8.0.0
 * Contact: support@hanzo.ai
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// BillingCapVerdict : The per-request spend-cap verdict for a (project, service) scope, evaluated over the org's spend-alert rows (most-restrictive-wins). Fails CLOSED: if a hard-enforceable row's spend cannot be computed the verdict denies. 
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct BillingCapVerdict {
    /// Whether the request is permitted.
    #[serde(rename = "allow")]
    pub allow: bool,
    #[serde(rename = "reason")]
    pub reason: models::BillingCapReason,
    /// The tightest violated cap's ceiling in USD cents (0 when allowed).
    #[serde(rename = "capCents", skip_serializing_if = "Option::is_none")]
    pub cap_cents: Option<i64>,
    /// Period spend for the scope in USD cents at decision time.
    #[serde(rename = "spentCents", skip_serializing_if = "Option::is_none")]
    pub spent_cents: Option<i64>,
    /// Utilization percent of the most-utilized covering cap when at/over its soft threshold (0 otherwise, and 0 on a deny).
    #[serde(rename = "warnPct", skip_serializing_if = "Option::is_none")]
    pub warn_pct: Option<i32>,
}

impl BillingCapVerdict {
    /// The per-request spend-cap verdict for a (project, service) scope, evaluated over the org's spend-alert rows (most-restrictive-wins). Fails CLOSED: if a hard-enforceable row's spend cannot be computed the verdict denies. 
    pub fn new(allow: bool, reason: models::BillingCapReason) -> BillingCapVerdict {
        BillingCapVerdict {
            allow,
            reason,
            cap_cents: None,
            spent_cents: None,
            warn_pct: None,
        }
    }
}

